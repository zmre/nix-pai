# Direnv configuration for Nix flakes
# This file activates the flake's devShell automatically

# Load the flake's development shell
# --accept-flake-config accepts nixConfig settings (substituters, etc.)
use flake . --accept-flake-config

# Optional: Add project-specific environment variables
# export MY_PROJECT_VAR="value"

# Optional: For unfree packages without nixpkgs-unfree input
# Uncomment the following lines:
# export NIXPKGS_ALLOW_UNFREE=1
# use flake . --accept-flake-config --impure
